// xNico Dashboard Configuration
window.DASHBOARD_CONFIG = {
    // Set to your API base URL if hosting frontend/backend separately.
    // Leave empty ('') when served from the same origin.
    API_BASE_URL: '',
    VERSION: '4.0.0'
};
